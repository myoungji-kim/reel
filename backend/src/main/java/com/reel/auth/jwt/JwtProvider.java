package com.reel.auth.jwt;

import org.springframework.stereotype.Component;

@Component
public class JwtProvider {
    // TODO: Phase 3 — generateAccessToken, generateRefreshToken, validateToken, extractUserId
}
