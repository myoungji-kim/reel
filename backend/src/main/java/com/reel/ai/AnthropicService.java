package com.reel.ai;

import org.springframework.stereotype.Service;

@Service
public class AnthropicService {
    // TODO: Phase 4, 5
    // - chat(messages): 채팅 응답 생성
    // - develop(conversation): 일기 생성 (title + content JSON 반환)
    // WebClient 사용, API 키는 application.yml anthropic.api-key
}
