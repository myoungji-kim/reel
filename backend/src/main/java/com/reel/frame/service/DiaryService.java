package com.reel.frame.service;

import org.springframework.stereotype.Service;

@Service
public class DiaryService {
    // TODO: Phase 5
    // - develop(userId, sessionId): AI 일기 생성 → DevelopPreviewResponse 반환
    // - save(userId, request): 일기 최종 저장 → DiaryResponse 반환
    // - findAll(userId): 전체 일기 목록 조회
    // - findById(userId, id): 일기 상세 조회
}
